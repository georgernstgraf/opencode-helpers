Open tasks:

1. [ ] dell (10.8.0.8, offline as of 2026-09-06) is another searxng consumer.
      When online, replicate the think setup: `git pull` in
      `~/repos/georgernstgraf/opencode-helpers`, point `mcp.searxng.command`
      in `~/.config/opencode/opencode.json` to
      `skills/searxng/scripts/opencode-searxng` (drop the `SEARX_URL` env),
      then verify `searxng-search.sh` reports `https://searxng.claw.graf.priv.at`.
      May need claw's SSH key (`georg@claw`) authorized on dell first.
      Reference setup: think (done 2026-09-06).

Last updated: 2026-09-06.
